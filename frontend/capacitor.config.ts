import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.diskmensagem',
  appName: 'Disk Mensagem',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
