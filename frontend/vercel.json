{
  "framework": "vite",
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm ci || npm install",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
